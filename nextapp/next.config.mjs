/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
      domains: [
        'images.pexels.com',
        'image.cnbcfm.com',
        'w0.peakpx.com'
      ],
    },
  }
  
export default nextConfig;
  